unit_tests:
  - name: test_fact_instance_enrollments
    model: fact_instance_enrollments
    config:
      tags: 'ci'
    given:
      - input: ref('int_enrollment_events')
        format: sql
        rows: |
          select * from int_enrollment_events
    expect:
      format: sql
      rows: |
        select * from fact_instance_enrollments_expected
