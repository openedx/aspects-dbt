version: 2

seeds:
  - name: xapi_events_all_parsed_expected
    config:
      column_types:
        emission_time: DateTime64(6)
        # use toTimeZone(emission_time, 'UTC') when populating seed file
        # daylight savings time messes up a few records so those manually adjusted