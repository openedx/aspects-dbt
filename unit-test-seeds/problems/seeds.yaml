version: 3

seeds:
  - name: dim_learner_response_attempt_expected
    config:
      column_types:
        first_success_at: Nullable(DateTime)
  - name: problem_events_expected
    config:
      column_types:
        scaled_score: Float64