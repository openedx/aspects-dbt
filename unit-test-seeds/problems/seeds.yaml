version: 2

seeds:
  - name: int_responses_expected
    config:
      column_types:
        first_success_at: Nullable(DateTime)
  - name: int_problem_events_expected
    config:
      column_types:
        scaled_score: Float64