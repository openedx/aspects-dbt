version: 2

seeds:
  - name: grading_events_expected
    config:
      column_types:
        scaled_score: Float64
  - name: dim_student_status_expected
    config:
      column_types:
        course_grade: Float64